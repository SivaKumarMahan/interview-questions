# Kubernetes Networking

- Services, `port`, `targetPort`, and `nodePort`
- Ingress controllers and cloud load balancers
- CoreDNS and service discovery
- CNI, Pod-to-Pod networking, kube-proxy, iptables, and IPVS
- NetworkPolicy and namespace isolation
- VPN/external database connectivity
- TCP/UDP services and port forwarding
- Service mesh, mTLS, and traffic policy
- Cross-cluster connectivity and DDoS protection
- Network and 503 troubleshooting

See `questions.txt`, `scenario-questions.txt`, `summary.txt`, and `notes.txt`.

