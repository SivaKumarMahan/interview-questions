# Docker Networking

- Container DNS troubleshooting
- Bridge and user-defined networks
- Container-to-container and host connectivity
- `EXPOSE` vs. published ports
- Nginx port mapping
- Namespace, route, firewall, and packet-capture investigation

See `questions.txt` and `notes.txt`.

`summary.txt` contains the screenshot-derived network-driver, bridge-flow, port-publishing, and container-DNS reference.
